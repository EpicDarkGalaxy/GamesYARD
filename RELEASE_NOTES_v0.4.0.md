# Release notes — v0.4.0 (draft)

This repository contains a non-production tag: `v0.4.0`.

What changed

- Minor improvements and README fixes.
- Not a production-ready build; use for testing and feedback.

How to get the build

- You can run the app from source:

```bash
git checkout v0.4.0
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python app.py
```

Notes

- If you'd like, I can add GitHub Actions to build PyInstaller artifacts and attach them to a release for download.
